<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/expense_tracker"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/expense_tracker_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project1">
        <activity android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- LOGIN - App yahan se shuru hoga -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">

        </activity> <!-- SIGNUP -->
        <activity
            android:name=".SignupActivity"
            android:exported="false" /> <!-- MAIN -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- SUMMARY -->
        <activity
            android:name=".SummaryActivity"
            android:exported="false" />
    </application>

</manifest>